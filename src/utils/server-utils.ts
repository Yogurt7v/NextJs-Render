export const serverSideFunction = () => {
    console.log(
        `use multiple libraries,
        use environment variables,
        interact with a databases,
        process confidential data`
    )
    return "server result in console"
}